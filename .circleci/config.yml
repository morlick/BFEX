version: 2
jobs:
  build:
    docker:
      - image: continuumio/miniconda

    steps:
      - run:
          name: Install docker-compose
          command: |
            curl -L https://github.com/docker/compose/releases/download/1.19.0/docker-compose-`uname -s`-`uname -m` > ~/docker-compose
            chmod +x ~/docker-compose
            sudo mv ~/docker-compose /usr/local/bin/docker-compose

      - setup_remote_docker

      - checkout

      - run:
          name: Build Docker Containers
          command: docker-compose build

      - run: 
          name: Run Docker Containers
          command: docker-compose up -d -f docker-compose.yml

      - run:
          name: Run tests on container
          command: sudo lxc-attach -n "$(docker inspect --format "{{.Id}}" bfex_bfex_1)" -- bash -c "cd /usr/app && /opt/conda/envs/BFEX/bin/py.test web/test"
      
      # - run:
      #     name: Setup Python Env for tests
      #     command: |
      #       set -x
      #       conda-env create -f ./web/environment.yml

      # - run:
      #     name: Run tests
      #     command: |
      #       set -e
      #       source activate BFEX
      #       pytest ./web

      - add_ssh_keys

      # - run:
      #     name: Add Known Hosts
      #     command: |
      #       set -x
      #       ssh-keyscan ${DEPLOY_IP} > ~/.ssh/known_hosts

      # - run:
      #     name: Deploy
      #     command: |
      #       set -x
      #       rsync -avr -e ssh --exclude=".*/" . ${DEPLOY_USER}@${DEPLOY_IP}:~/BFEX
      #       ssh ${DEPLOY_USER}@${DEPLOY_IP} << EOF
      #         cd BFEX;
      #         sudo docker-compose down --remove-orphans
      #         sudo docker-compose build
      #         sudo docker-compose up -d
      #       EOF
